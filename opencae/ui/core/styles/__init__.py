from . import base, buttons, docks, fields, menus, misc, scrollbars, tabs, views

STYLE_MODULES = (
    base,
    menus,
    docks,
    views,
    fields,
    tabs,
    buttons,
    misc,
    scrollbars,
)
